# Dependencies
node_modules/

# pnpm
pnpm-lock.yaml

# CDK
pkgs/cdk/cdk.out/
cdk-outputs.json
*.d.ts
!*.d.template.ts

# Build output (site bundle is produced at deploy by CDK)
pkgs/site/dist/
pkgs/site/app.js

# Local env / secrets
.env
.env.*

# OS
.DS_Store
**/.DS_Store

# Locally-generated static seller payTo (regenerate by deleting)
pkgs/cdk/.seller-payto.json

# Local MCP server config (per-developer, not shared)
.mcp.json

# Holmes scan artifacts (local)
.holmes/
